apiVersion: v1
kind: Service
metadata:
  name: web-service
  namespace: application
spec:
  type: NodePort
  selector:
    app: employees
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30000